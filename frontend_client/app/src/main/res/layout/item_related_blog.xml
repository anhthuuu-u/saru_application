<?xml version="1.0" encoding="utf-8"?>
<androidx.cardview.widget.CardView
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="180dp"
    android:layout_height="wrap_content"
    android:layout_margin="8dp"
    app:cardCornerRadius="8dp"
    app:cardElevation="4dp"
    app:cardBackgroundColor="@color/white">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:padding="8dp"
        android:background="@color/white"> <!-- Thêm background để kiểm tra -->

        <ImageView
            android:id="@+id/imgRelatedArticle"
            android:layout_width="match_parent"
            android:layout_height="100dp"
            android:scaleType="centerCrop"
            android:background="@color/black"
            android:contentDescription="Related Article Image" />

        <TextView
            android:id="@+id/txtRelatedTitle"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:textSize="14sp"
            android:textStyle="bold"
            android:textColor="@color/highlight_yellow"
            android:maxLines="2"
            android:ellipsize="end"
            android:layout_marginTop="8dp"
            android:text="Test Title" /> <!-- Thêm text mặc định để kiểm tra -->

        <TextView
            android:id="@+id/txtRelatedCategory"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:textSize="12sp"
            android:textStyle="italic"
            android:textColor="@color/highlight_yellow"
            android:layout_marginTop="4dp"
            android:text="Test Category" /> <!-- Thêm text mặc định để kiểm tra -->
    </LinearLayout>
</androidx.cardview.widget.CardView>